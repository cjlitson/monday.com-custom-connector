{
  "query": "query GetMondayItemDetails($itemIds: [ID!]!) { items(ids: $itemIds) { id name board { id name columns { id title type } } group { id title } parent_item { id name } column_values { id text value type } } }",
  "variables": {
    "itemIds": [
      "<ITEM_ID>"
    ]
  }
}
